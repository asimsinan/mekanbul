{
  "name": "mekanbul_mern",
  "version": "2.0",
  "private": true,
  "dependencies": {
    "concurrently": "latest",
    "express-jwt": "^8.4.1"
  },
  "scripts": {
    "start": "npm install && concurrently \"cd ./backend && npm install && nodemon\" \"cd ./frontend && npm install && npm run dev\""
  }
}
